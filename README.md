# Test ManaWeb
## Install
* Clone the repo
* Copy .env.example and rename to .env. Set your env variables
* Go to the project directory and run `composer install` then `npm install`
* Launch the servers `npm run dev` and `php artisan serve` on a different terminal

Go to http:127.0.0.1:8000 to see the app